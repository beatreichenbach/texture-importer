global proc textureimporter_setup() {
	$cwd = dirname(substitute(".+: ", whatIs("textureimporter_setup"), ""));
	$code = "import sys; sys.path.append('" + $cwd + "'); import setup_maya; setup_maya.install()";
	python($code);
}
textureimporter_setup;
